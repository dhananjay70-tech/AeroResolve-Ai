Redesign the AeroResolve AI frontend to look like a premium, highly realistic production-grade airline operations platform — NOT a simple chatbot UI.

IMPORTANT:
- Do NOT use or import any reference image.
- Generate the visual design completely from scratch.
- Keep all existing backend APIs, authentication, LangGraph integration, Node.js integration, escalation APIs, and business logic unchanged.
- Frontend only.
- JavaScript only, no TypeScript.

DESIGN DIRECTION:

Create a cinematic dark 3D airline command-center interface inspired by modern futuristic SaaS products.

BACKGROUND:
- Deep black / navy background.
- Add a realistic 3D airport/airline environment in the background.
- Subtle futuristic airport terminal, runway, aircraft silhouettes, navigation lights, flight paths and atmospheric glow.
- Use layered depth, blur, perspective and lighting.
- Add subtle animated particles and moving flight-path lines.
- Background must feel like a real 3D environment, not a flat gradient.
- Keep content readable with dark overlays.
- Do not make the background distracting.

HERO / MAIN AREA:
Create a strong premium hero section:

"AeroResolve AI"
"Autonomous Airline Resolution Agent"

Subtitle:
"Resolve disruptions, execute approved actions, and escalate complex cases — in real time."

Show a realistic 3D aircraft/airport visualization on the right side.

Add floating glass panels around the aircraft showing REAL application data:

- Flight Status
- PNR
- Delay / Cancellation
- Passenger Tier
- Resolution Status
- Agent Status

Example visual cards:

"SK-204"
"DEL → GOA"
"CANCELLED"

"AGENT ACTIVE"

"Gold Member"

"Resolution Ready"

Do NOT hardcode these values in the actual application.
Use existing backend data dynamically.

AGENT COMMAND CENTER:
Make the main application look like an actual AI operations console.

Desktop layout:

LEFT:
Case Context
- Passenger
- PNR
- Loyalty Tier
- Flight
- Route
- Current disruption
- Scheduled time
- Updated time

CENTER:
Agent Workspace
- Agent state machine
- Current decision
- Recommended allowed actions
- Execution status
- Customer conversation

RIGHT:
Tool Runtime
- Customer Lookup
- Booking Lookup
- Policy Check
- Action Execution
- Escalation
- Supervisor Call

Use realistic animated execution states:

○ Waiting
◉ Running
✓ Completed
⚠ Requires Supervisor

AGENT STATE MACHINE:

UNDERSTAND
↓
CONTEXT
↓
VERIFY
↓
POLICY
↓
DECIDE
↓
ACT
↓
VERIFY RESULT

Animate the current state based on real API activity.

3D VISUAL LANGUAGE:
- Glassmorphism
- Realistic depth
- Soft shadows
- Neon violet / blue / cyan highlights
- Subtle reflections
- Perspective cards
- Floating panels
- Hover depth
- Smooth Framer Motion animations
- Very subtle parallax
- Realistic lighting
- Avoid excessive glow

AIRLINE VISUALIZATION:
Create a small interactive 3D-style flight visualization:
- aircraft icon
- origin airport
- destination airport
- route line
- disruption indicator
- scheduled vs updated time
- status indicator

Do NOT create fake map data.
Use actual booking/flight data from the backend.

AGENT DECISION CARD:

Show:

"Agent Decision"

Then dynamically display the policy-approved resolution.

Example:

"Free rebooking available"
"Full refund available"

or

"Meal voucher + lounge access"

or

"Meal voucher + lounge + hotel"

Only display actions returned/allowed by the backend.
Never calculate airline policy independently in React.

ACTION CENTER:

Create premium action cards:

REBOOK FLIGHT
FULL REFUND
MEAL VOUCHER
LOUNGE
HOTEL
ESCALATE

Only show actions allowed for the current case.

When clicked:
- confirmation modal
- API request
- loading animation
- success/failure state
- update timeline
- update dashboard

Never fake success.

SUPERVISOR ESCALATION:

When escalation is required:

Show:

"Supervisor Review Required"

with:

- escalation reason
- customer
- PNR
- requested exception
- escalation status
- supervisor call status

If the backend reports supervisor call initiated:

"Calling Supervisor..."

Then:

"Supervisor Call Initiated"

Do not fake call status.

AGENT TRACE:

Add a "View Agent Trace" button.

Open a beautiful side drawer showing observable execution events:

[✓] Customer context loaded
[✓] Booking verified
[✓] Policy checked
[✓] Resolution determined
[✓] Action executed
[✓] Result verified

Do NOT expose hidden chain-of-thought or internal reasoning.
Only show safe tool/event information.

CUSTOMER CHAT:

Chat should be secondary, not the entire UI.

Make it look like a communication panel inside an agent operations console.

Include:
- customer messages
- agent messages
- typing state
- action cards
- escalation banner
- resolution confirmation

REAL DATA ONLY:

Use the existing:
- AuthContext
- api.js
- Axios
- React Router
- backend APIs
- agent AI endpoint
- escalation endpoint

Do not create mock APIs.
Do not create fake agent events.
Do not expose API keys in frontend.

RESPONSIVE:

Desktop:
3-column command center.

Tablet:
2-column layout.

Mobile:
single-column stacked layout with:
1. Case summary
2. Agent state
3. Decision
4. Actions
5. Tool runtime
6. Chat
7. Trace

Optimize for 390px width.

VISUAL QUALITY:

The final result should feel like:

"Airline AI Operations Command Center"

not:

"ChatGPT clone"
not:
"simple dashboard"
not:
"generic admin panel"

Use premium spacing, typography, realistic depth, 3D background environment, subtle animation and high-quality glass panels.

IMPORTANT:
Do not replace existing functionality.
Do not modify backend logic.
Do not change API contracts.
Do not use fake data.
Do not use TypeScript.

After implementation:
- run npm run build
- fix all errors
- verify desktop 1440px
- verify tablet 768px
- verify mobile 390px
- confirm existing login, booking, AI chat and escalation flows still work.