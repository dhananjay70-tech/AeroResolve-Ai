You are working on my existing AeroResolve AI Node.js + Express backend.

IMPORTANT:
- JavaScript only. NO TypeScript.
- Do NOT break any existing authentication, Drizzle ORM, PostgreSQL/Supabase, agent chat, escalation, or policy logic.
- Do not modify the Python AI service.
- Exotel credentials must NEVER be exposed to frontend or AI service.
- Never hardcode real credentials.
- Use process.env for all Exotel secrets.
- Inspect the existing backend structure before making changes.

TASK: Add complete Exotel outbound calling integration to the Node.js backend.

1. UPDATE Backend/.env

Add these variables if they do not already exist:

EXOTEL_ACCOUNT_SID=
EXOTEL_API_KEY=
EXOTEL_API_TOKEN=
EXOTEL_CALLER_ID=
SUPERVISOR_PHONE_NUMBER=
EXOTEL_API_BASE_URL=

Do NOT put fake real credentials in these variables.
Leave values empty if I have not obtained them yet.

2. UPDATE Backend/.env.example

Add the same variables with safe placeholders:

EXOTEL_ACCOUNT_SID=your_exotel_account_sid
EXOTEL_API_KEY=your_exotel_api_key
EXOTEL_API_TOKEN=your_exotel_api_token
EXOTEL_CALLER_ID=your_exotel_phone_number
SUPERVISOR_PHONE_NUMBER=+91XXXXXXXXXX
EXOTEL_API_BASE_URL=https://api.in.exotel.com

3. CHECK .gitignore

Make sure:
.env
.env.*
!.env.example

is handled correctly so real Exotel credentials can NEVER be committed.

4. CREATE EXOTEL SERVICE

Create:

src/services/exotelService.js

The service must:

- Read credentials only from process.env
- Validate that required credentials exist before attempting a call
- Never log API keys, API tokens, account SID, or phone numbers
- Normalize Indian phone numbers to +91XXXXXXXXXX where appropriate
- Provide a function:

startSupervisorCall({ escalationId, customerName, pnr, issue, summary })

The function should make the Exotel outbound call using the official Exotel API format.
Use the current official Exotel API documentation rather than inventing endpoints or parameters.

The call should be made FROM:
EXOTEL_CALLER_ID

TO:
SUPERVISOR_PHONE_NUMBER

5. DO NOT FAKE CALL SUCCESS

If:
- Exotel credentials are missing
- ExoPhone/caller ID is missing
- supervisor number is missing
- Exotel API returns an error
- account is not enabled for outbound calling

then return a structured error.

Example:

{
  success: false,
  status: "NOT_CONFIGURED",
  message: "Exotel calling is not configured yet."
}

Never return fake "call started" success.

6. CREATE CONTROLLER

Create or update:

src/controllers/escalationController.js

When an escalation is created successfully, support starting the supervisor call.

Keep escalation creation working even if Exotel is not configured.

For example:

create escalation
      ↓
save escalation in PostgreSQL
      ↓
attempt Exotel supervisor call
      ↓
save call status if possible
      ↓
return escalation response

Exotel failure must NOT delete or rollback the escalation.

7. CREATE API ENDPOINT

Add a protected endpoint such as:

POST /api/escalations/:id/call-supervisor

Requirements:
- JWT authentication
- Validate escalation exists
- Use the escalation/customer/booking data already stored in the database
- Call exotelService.startSupervisorCall(...)
- Return clean JSON
- No secrets in response
- No fake success

Example response:

{
  "success": true,
  "message": "Supervisor call initiated",
  "escalationId": "...",
  "callStatus": "initiated"
}

If Exotel is not configured:

{
  "success": false,
  "message": "Supervisor calling is not configured yet",
  "callStatus": "not_configured"
}

8. AUTOMATIC ESCALATION FLOW

Inspect the existing agent/escalation flow.

If the existing backend already creates an escalation when the customer asks for a supervisor/manager, DO NOT duplicate that logic.

After a successful escalation is created, integrate the supervisor call into that existing flow.

Desired architecture:

Customer
 ↓
React
 ↓
Node /api/agent/ai-chat
 ↓
Python AI
 ↓
Node controlled tools
 ↓
Policy / escalation
 ↓
PostgreSQL
 ↓
Exotel
 ↓
Supervisor +91 number

9. KEEP AI CONTROLLED

The Python AI service must NOT receive Exotel credentials.

The AI must NOT directly call Exotel.

Only Node.js backend can call Exotel.

10. ADD LOGGING

Use safe logs such as:

[Exotel] Supervisor call requested
[Exotel] Calling supervisor for escalation <id>
[Exotel] Call initiated successfully
[Exotel] Call failed

NEVER log:
- API key
- API token
- account SID
- full phone numbers

11. ADD HEALTH/CONFIG STATUS

Add a safe internal configuration check if appropriate:

GET /api/escalations/exotel/status

Response should only show:

{
  "configured": true/false,
  "callerIdConfigured": true/false,
  "supervisorNumberConfigured": true/false
}

Never expose actual credentials.

12. DATABASE

Inspect the existing escalation schema.

If there is already a suitable field for call status, reuse it.

If absolutely necessary, add fields for:

callStatus
callId
callStartedAt

Do not unnecessarily change the database schema.

If a migration is required, create the proper Drizzle migration.

13. ERROR HANDLING

Handle:
- 400 validation errors
- 401 unauthorized
- 404 escalation not found
- Exotel 4xx
- Exotel 5xx
- timeout
- missing credentials
- account not enabled
- invalid phone number

Return clean JSON responses.

Do not expose raw Exotel credentials or internal stack traces.

14. TESTING

After implementation:

- npm run build
- run existing backend tests
- verify server starts
- verify existing agent chat still works
- verify existing escalation creation still works
- verify Exotel status endpoint
- test missing Exotel credentials
- test that no real call is attempted when credentials are missing

Do NOT make a real phone call during testing unless explicitly requested.

15. FINAL OUTPUT

After completing the changes, show me:

1. Files created
2. Files modified
3. Exact new environment variables
4. API endpoints added
5. How the escalation → Exotel call flow works
6. Commands to run the backend
7. Any migration command required
8. Whether Exotel credentials/caller ID are still missing

IMPORTANT:
Do not invent Exotel credentials, phone numbers, call IDs, or successful call responses.
Use the official Exotel API format.